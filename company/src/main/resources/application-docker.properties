# Docker-specific configuration for Company Service
server.port=8081
spring.application.name=company

# Database Configuration - Using host PostgreSQL from Docker container
spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/company
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# RabbitMQ Configuration - Using Docker service name
spring.rabbitmq.host=rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Logging
logging.level.com.example.company=DEBUG
logging.level.org.springframework.amqp=DEBUG
